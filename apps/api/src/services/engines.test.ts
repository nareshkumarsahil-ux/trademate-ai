import{describe,it,expect}from'vitest';import{movers,topGainers,momentum,breakout,validatePaperOrder,type ScanQuote}from'./engines.js';
const q=(changePercent:number,extra:Partial<ScanQuote>={}):ScanQuote=>({symbol:'TEST',changePercent,relativeVolume:2,intradayRangePercent:3,distanceFromHighPercent:1,aboveShortMA:true,price:102,...extra});
describe('scanner engines',()=>{it.each([2,5,10])('filters %s%% movers',t=>expect(movers([q(t-.01),q(t)],t)).toHaveLength(1));it('sorts gainers',()=>expect(topGainers([q(2,{symbol:'A'}),q(5,{symbol:'B'})])[0].symbol).toBe('B'));it('scores momentum transparently',()=>{const m=momentum(q(4));expect(m.score).toBeGreaterThan(70);expect(m.parts.trend).toBe(15)});it('confirms breakout with volume',()=>expect(breakout(q(3,{breakoutLevel:100}),1.5,.2)).toBe(true))});
describe('paper simulation',()=>{it('records a buy without broker execution',()=>expect(validatePaperOrder({symbol:'ABC',side:'BUY',quantity:10,price:100},100000).simulationOnly).toBe(true));it('rejects excess capital',()=>expect(()=>validatePaperOrder({symbol:'ABC',side:'BUY',quantity:10,price:100},500)).toThrow())});
